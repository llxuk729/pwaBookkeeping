# PWA 本地 AI 智能记账系统后续开发方案

## 核心方向

项目定位：

* Local First
* 纯前端
* GitHub Pages 部署
* 数据全部本地存储
* 离线优先
* AI 辅助而非 AI 主导

核心目标：

* 快
* 稳
* 低功耗
* 可维护
* 移动端友好

---

# 一、Parser 架构

继续采用：

```text
Normalize
→ Tokenize
→ Item Splitter
→ Money FSM
→ Structured Parser
→ Validator
```

已完成：

* Money FSM
* 中文金额 DSL
* Item Splitter

后续重点：

* 增加测试 case
* 提高 normalize 覆盖率
* 增强口语表达兼容
* 增强多 item 稳定性

---

# 二、AI 调整方向

不再使用：

* embedding 作为 parser 核心
* LoRA
* instruction llm 主解析

bge-small-zh-v1.5 仅保留用于：

* 分类
* 标签建议
* 相似记录推荐

AI 不负责：

* 金额计算
* 中文数字解析
* JSON结构生成

这些全部由规则系统完成。

---

# 三、语音输入方案

采用“双引擎方案”。

## Android

优先：

```text
SpeechRecognition
```

失败 fallback：

```text
whisper-tiny
```

原因：

* 速度快
* 几乎零功耗
* 不下载模型
* Android Chrome 兼容很好

---

## iOS PWA

直接使用：

```text
MediaRecorder
→ whisper-tiny
```

不依赖：

```text
SpeechRecognition
```

原因：

* iOS PWA 对 Web Speech API 支持不稳定
* WebKit 限制较多

---

# 四、语音架构

建立：

```text
speech-engine/
```

抽象 provider：

```ts
interface SpeechProvider {
  start(): void
  stop(): Promise<string>
}
```

实现：

* speech-recognition-provider
* whisper-provider

---

# 五、Whisper 使用方案

使用：

* transformers.js
* whisper-tiny

要求：

* lazy load
* 按需初始化
* 不实时转写
* 录音结束后一次识别
* 推理完成后释放资源

交互：

```text
按住说话
→ 松开
→ 转文字
→ parser
```

---

# 六、PWA 技术栈

前端：

* Vue3
* Vite
* vite-plugin-pwa

数据库：

* Dexie.js
* IndexedDB

数据校验：

* Zod

---

# 七、重点开发方向

核心资产：

* 中文金额解析引擎
* Normalize 系统
* 口语结构化输入
* 离线语音记账体验

而不是：

* 大模型
* Agent
* LangChain
* LoRA

---

# 八、最终产品定位

不是：

```text
AI 聊天记账
```

而是：

```text
离线本地 AI 语音结构化输入工具
```

核心卖点：

* 离线
* 隐私
* 极速
* 本地存储
* 无账号
* 小商户/家庭/仓库场景
* 低学习成本
